<% if current_user.is_admin? %>
  <button type="button" class="btn btn-light w-100">
    <h1>クイズ新規登録</h1>

    <%= render 'form', quiz: @quiz %>

    <%= link_to 'クイズ一覧に戻る', quizzes_path ,class: 'btn btn-primary' %>
  </button>
<% end %>