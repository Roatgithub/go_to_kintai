<div class="w-100 d-flex flex-column align-items-center"> 
  <h4>企業 <%= @user.company %></h4>
  <h4><%= @user.name %></h4>
  <h4><%= @user.email %></h4>
  <%= render 'shared/date' %>
</div>

<%= button_to '打刻する', {:controller => "attendances", :action => "create"}, class: "btn btn-primary w-100 my-3", style:"height: 150px" %>

<nav class="navbar navbar-expand-lg btn-group w-100 mb-3" role="group" aria-label="Basic outlined example">
  <%= link_to '勤怠状況一覧', attendances_path, class: 'btn btn-outline-primary btn-lg' %>
  <% if current_user.is_admin? %>
    <%= link_to 'クイズ一覧', quizzes_path, class: 'btn btn-outline-primary btn-lg' %>
  <% end %>
  <%= link_to 'ユーザー情報編集', edit_user_path, class: 'btn btn-outline-primary btn-lg' %>
</nav>